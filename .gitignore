# deps & build
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.vite/

# env & secrets —— 永远不要进仓
.env
.env.*
!.env.example

# 本地状态 / 会话转储 / 归档
*.session.json
my-session.json
*.zip
*.tar.gz
*.log

# memory 本地设计文档（Spec + 代码导读 + 模块导读），不入仓
src/memory/docs/
src/memory/README.md

# Local-only docs（路径清单见 .githubignore，供 AI 助手与本地参考，不入仓）
doc/
.github/pr-body.md

# editor & OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
